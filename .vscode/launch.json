{
    "version": "0.1.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                "/var/www/skyticket.com": "${workspaceFolder}/src"
            }
        }
    ]
}
